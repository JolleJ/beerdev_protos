name: Copy
on: push
jobs:
  copy:
    runs-on: ubuntu-latest
    steps:
    - name: Copycat
      uses: andstor/copycat-action@v3
      with:
        personal_token: ${{ secrets.API_TOKEN_GITHUB2 }}
        src_path: /protos
        dst_path: /
        dst_owner: JolleJ
        dst_repo_name: testAction
        dst_branch: master
        src_branch: master
